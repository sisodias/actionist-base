import type { BlockMount, Health } from './host';

export const fixtureBlock: BlockMount = { id: 'actionist/fixture', version: '0.1.0', route: '/fixture', capability: 'fixture.view', label: 'Fixture', async mount(target) { target.innerHTML = '<div data-verify="fixture-block"><strong>Native fixture block</strong><p>Mounted through the host lifecycle.</p></div>'; return () => { target.replaceChildren(); }; }, health: async (): Promise<Health> => ({ status: 'healthy' }) };

export function affineBlock(moduleUrl = ''): BlockMount { return { id: 'actionist/affine-workspace', version: '0.1.0', route: '/knowledge', capability: 'knowledge.view', label: 'Knowledge', async mount(target, ctx, bindings) { if (!moduleUrl || !bindings.apiBaseUrl) { target.innerHTML = '<div data-verify="block-unavailable"><h2>Knowledge unavailable</h2><p>A compiled AFFiNE module URL and backend binding are required.</p></div>'; return () => target.replaceChildren(); } const mod = await import(/* @vite-ignore */ moduleUrl) as { mount?: (el: HTMLElement, options: unknown) => Promise<() => void> | (() => void) }; if (!mod.mount) throw new Error('AFFiNE module does not export mount'); return await mod.mount(target, { context: ctx, bindings }); }, health: async (_ctx, bindings) => !moduleUrl || !bindings.apiBaseUrl ? { status: 'unavailable', detail: 'missing module URL or backend binding' } : { status: 'healthy' } }; }
